$(document).ready(() => {
  // REMEMBER: Especially for Materialize CSS, only place inits for code
  //  existing in index.html and not those dynamically-generated later by
  //  Angular; place those in Angular's controllers, etc.
  
  // Materialize CSS inits for hardcoded elements, etc.
});